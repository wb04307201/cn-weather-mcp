spring:
  main:
    web-application-type: none
    banner-mode: off
  ai:
    mcp:
      server:
        name: cn-weather-mcp
        version: 1.0.0
logging:
  pattern:
    console:
  file:
    name: ./mcp/cn-weather-mcp.log
