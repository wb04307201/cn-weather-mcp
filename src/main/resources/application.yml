spring:
  main:
    web-application-type: none
    banner-mode: off
  ai:
    mcp:
      server:
        name: cn-weather-mcp
        version: 0.0.1
logging:
  pattern:
    console:
  file:
    name: ./cn-weather-mcp.log
